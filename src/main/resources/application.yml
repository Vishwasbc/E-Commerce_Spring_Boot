spring:
  application:
    name: ECommerce
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/ECommerce_DB?createDatabaseIfNotExist=true"
    username: root
    password: "abz62#S&eQ58"
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  app:
    jwtSecret: "mySecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm"
    jwtExpirationMs: 300000000
    cookieName: SpringBootECom

server:
  port: 8001

# Optional logging flags (uncomment to enable)
# logging:
#   level:
#     org.hibernate.sql: DEBUG
#     org.springframework.security: DEBUG
#     com.ecommerce: DEBUG
